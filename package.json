{
  "name": "microfrontend",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "dev": "turbo run dev",
    "build": "turbo run build"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.6.3"
  }
}
